<h1>New job</h1>
<%= form_tag(jobs_path) do |f| %>
  <div class="field">
    <label>Name</label><br />
    <%= text_field_tag :name %>
  </div>
  <div class="actions">
    <%= submit_tag 'launch' %>
  </div>
<% end %>
<%= link_to 'Back', jobs_path %>
